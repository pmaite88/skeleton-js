import HomeButton from '../components/HomeButton';
import Item from '../components/Item';

export default () => (
  <div>
    Third Page - You can see this without logging in!
    <HomeButton />
  </div>
);
